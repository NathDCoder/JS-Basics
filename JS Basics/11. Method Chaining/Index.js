/* Method chaining is the process of calling one method after another 
  continuously in one line of code. */

  let userName = "JeanValJean"

  let letter = userName.charAt(0).toUpperCase().trim();

  console.log(letter);